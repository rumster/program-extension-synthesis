factorial(x:int) -> (y:int) {
  example {
    [x==1] ->
    y = 1
  }

  example {
    [x==2] ->
    y = 1 ->
    x = x - 1 ->
    y = y * x
  }

  example {
    [x==3] ->
    y = 1 ->
    x = x - 1 ->
    y = y * x ->
    x = x - 1 ->
    y = y * x
  }
}
