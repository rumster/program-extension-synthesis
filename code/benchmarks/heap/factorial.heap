factorial(mut x:int) -> (y:int) {
  y = 1;
  while (x != 1) {
    y = y * x;
    x = x - 1;
  }

  example {
    [x==0]
    -> y = 1;
  }

/*
  example {
    [x==1]
    -> y = 1;
  }

  example {
    [x==2]
    -> y = 1;
    -> y = y * x;
    -> x = x - 1;
  }
  */

  example {
    [x==6]
    -> y = 1;
    -> [y==720]
  }

  example {
    [x==3]
    ->  y = 1;
    ->  y = y * x;
    ->  x = x - 1;
    ->  y = y * x;
    ->  x = x - 1;
  }
}
